"""FinStream dashboard: a read-only Streamlit view over the raw market data.

This service only ever reads. Transformations and derived series belong to a processing
service (AGENTS.md GR-1, ADR-0001).
"""

__version__ = "0.1.0"
