"""FinStream Ingestor: extracts market data from Yahoo Finance and loads it raw into TimescaleDB.

Extract & Load only. Transformations belong to downstream services (AGENTS.md GR-1, ADR-0001).
"""

__version__ = "0.1.0"
